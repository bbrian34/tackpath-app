import test from 'node:test';import assert from 'node:assert/strict';import {censusGeocode} from '../supabase/functions/_shared/geocode.js';
test('fallback accepts one matching street-range address and rejects ambiguous or wrong ZIP results',async t=>{const original=globalThis.fetch;t.after(()=>globalThis.fetch=original);const match={matchedAddress:'225 BAKER ST NW, ATLANTA, GA, 30313',addressComponents:{zip:'30313',state:'GA',streetName:'BAKER'},coordinates:{x:-84.394,y:33.762}};let matches=[match];globalThis.fetch=async()=>Response.json({result:{addressMatches:matches}});assert.equal((await censusGeocode('225 Baker St NW, Atlanta, GA 30313')).provider,'us_census');matches=[match,match];await assert.rejects(censusGeocode('225 Baker St NW, Atlanta, GA 30313'),/ambiguous/);matches=[match];await assert.rejects(censusGeocode('225 Baker St NW, Atlanta, GA 30314'),/correction/);await assert.rejects(censusGeocode('Atlanta GA'),/complete U.S./);});
